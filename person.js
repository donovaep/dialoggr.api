// Defines a Mongoose model for a person
Let mongoose = require('mongoose');
Let Schema = mongoose.Schema;
Let PersonSchema - new Schema({

});
module.exports = mongoose.model('Person', PersonSchema);